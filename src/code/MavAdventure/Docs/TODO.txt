+++ Управление
(
	+++ настраиваемое
)
+++ Бэки 
(
	+++враппинг
	+++скроллинг
	//анимация 
	+++флаг фореграунда
)
- мин-максы для оптимизации рендера
+++ Экспанды
// - Трейс по тайлам
// (
	// +++вниз
	// ---вверх
	// ---влево
	// ---вправо
// )
 +++ Флаги тайлов в таил-картах 
(
	// твердость          ...шейдер +++код
	// анимированность    ---шейдер ---код
	флип(x,y)          +++шейдер +++код
	повернутость-на-90 +++шейдер +++код
) 
+++ Функция прверки флага в шейдере
+++ Переходы между комнатами/уровнями
+++ Лэндмарки
+++ Триггеры перехода
// - Ручной рендер текста
// (
	// --- текстура под шрифт 16*maxW-X-16*H
	// --- отчистка текстуры черным по альфе 0
	// --- массив ширины букв на 256 элементов
	// --- последовательное рисование со смещением Х на ширину текущей буквы
// )
+++ сортировка по глубине всего вместе (тайлы+объекты)
+++ Эмуляция вида для ограничения скроллинга
+++ Возможность ограничения скорости скроллинга
+++ Автоскроллинг
+++ Большие диалоги
(
	 +++ автопечать текста с переносами
	 +++ автоадванс
	 +++ ручной адванс
	 --+ система ускорения текста при ручном адвансе
	 (
		удержание А\В\С- ускорение вывода
		нажатие А\В\С  - адванс
		при отчистке диалога - перезажать А\В\С
	 )
	 портрет:  +++слева  +++справа
)
- Пауза
(
	//пауза только в игре, блокировка паузы при переходах, отсутствие паузы в меню и т.п.
	игровой объект, разрешающий паузу. При его оцуцтвии пауза снимаецо автоматически
)
- Блокировка управления
(
	--- при диалогах
)
- Класс выполнения с задержкой (ака logic_relay)